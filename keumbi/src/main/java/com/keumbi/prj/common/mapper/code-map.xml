<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.keumbi.prj.common.mapper.CodeMapper">
	<select id="keywordCode" resultType="CodeVO">
		SELECT	CODE, VAL
		FROM	COMMON_CODE
		WHERE	PRE_CODE = 'K' 
	</select>
	
	<select id="categoryCode" resultType="CodeVO">
		SELECT	CODE, VAL
		FROM	COMMON_CODE
		WHERE	PRE_CODE = 'D' 
	</select>
	
	<select id="bankCode" resultType="CodeVO">
		SELECT DISTINCT(SUBSTR(c.code,2,3)) AS code, val
		FROM PRD_DEP_BASES b
		JOIN COMMON_CODE c
		ON B.KOR_CO_NM = C.VAL
		WHERE B.KOR_CO_NM = ${kor_co_nm}
	</select>
</mapper>