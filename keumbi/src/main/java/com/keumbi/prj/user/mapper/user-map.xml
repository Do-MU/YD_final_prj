<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.keumbi.prj.user.mapper.UserMapper">
	<select id="userSelectList" resultType="UserVO">
		SELECT	*
		FROM	USERS
	</select>
	
	<select id="userSelect" resultType="UserVO">
		SELECT	*
		FROM	USERS
		WHERE	ID = #{id}
       	AND		PW = #{pw}
	</select>
	
	<select id="userCheck" resultType="UserVO">

	</select>
	
	<select id="userFind" resultType="UserVO">
		SELECT	ID
		FROM	USERS
		WHERE	NAME = #{name}
		AND		EMAIL = #{email}
	</select>
		
	<insert id="userInsert" parameterType="int">
		INSERT INTO USERS ( ID, PW, NAME, BIRTH, GENDER_CODE, ADDR, PHONE, USER_CODE )
	</insert>
	
	<update id="userUpdate" parameterType="int">
	
	</update>
	
	<delete id="userDelete" parameterType="int">
	
	</delete>
	
	<select id="selectToken"></select>
	
	<update id="UpdateToken" parameterType="UserVO">
		UPDATE USERS
		SET USER_SEQ_NUM = #{user_seq_num}, ACCESS_TOKEN = #{access_token}, REFRESH_TOKEN = #{refresh_token}
		WHERE ID = #{id}
		
	</update>
</mapper>